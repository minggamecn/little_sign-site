import { PolicyPage, policyMetadata } from '../../PolicyPage';
export const metadata = policyMetadata('support', 'zh');
export default function Page() { return <PolicyPage kind="support" locale="zh" />; }
